# 🌍 Global Weather Fetcher CLI

A lightweight, command-line Python tool that fetches real-time weather data for any city worldwide using the `wttr.in` service.

## 📖 Overview
This project demonstrates how to interact with REST APIs using Python. It sends HTTP requests to a public weather service, processes the JSON response, and displays the current temperature and weather conditions in a clean, readable format.

## 🚀 Features
- **Real-Time Data:** Instantly fetches current temperature (°C) and weather descriptions.
- **Global Coverage:** Supports any city name (e.g., Athens, London, Tokyo).
- **JSON Parsing:** Efficiently extracts specific data points from raw JSON responses.
- **Error Handling:** Robustly handles network errors or invalid city names without crashing.
- **Zero Configuration:** No API keys or complicated setup required.

## 🛠️ Tech Stack
- **Language:** Python 3.x
- **Libraries:** `requests`
- **API:** wttr.in (JSON format)

## 📦 Installation

1. **Clone or Download the repository:**
   ```bash
   git clone [https://github.com/your-username/weather-fetcher.git](https://github.com/your-username/weather-fetcher.git)
   cd weather-fetcher